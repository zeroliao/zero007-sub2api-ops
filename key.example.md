AbuseIPDB API Key: replace-with-your-api-key

Recommended production configuration:

```text
ABUSEIPDB_API_KEY=replace-with-your-api-key
```

Then configure proxy subscription sources to use:

```text
env:ABUSEIPDB_API_KEY
```

Do not commit the real `key.md` file.

