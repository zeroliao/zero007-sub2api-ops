# v010 修复 Clash 订阅 YAML 缩进

## 范围

- ops 版本：`release/010`。
- 源码版本：沿用线上应用镜像，本次仅修复 Clash 订阅配置生成模板。

## 变更

- 修复完整 Clash 订阅中 `proxies` 列表项缩进，避免 FlClash / Mihomo 解析时报错：
  `yaml: line 45: mapping values are not allowed in this context`。

## 验证

- 本地 `C:\Users\Administrator\Downloads\日本` 使用 PyYAML 解析通过。
- 本地 `C:\Users\Administrator\Downloads\美国` 使用 PyYAML 解析通过。
- ops 完整订阅模板使用占位变量渲染后，PyYAML 解析通过。
- `validate-candidate`：通过，候选提交 `3ea569fe0ca652451435e53d5eb9fc95a960a330`。
- 发布前备份：完成，备份目录 `/opt/sub2api-deploy/backups/20260601T092521Z`。
- 蓝绿部署：通过，运行 `20260601T092548Z-bluegreen-deploy-1993837`，`exit_code=0`。
- 服务器实际生成的完整订阅使用 PyYAML 解析通过，`proxies=1`，`rules=24`。
- 服务器实际生成文件确认 `proxies` 第一个列表项缩进为两个空格，子字段缩进为四个空格。
- 线上完整订阅返回 `HTTP 200`，`Content-Type: text/yaml; charset=utf-8`。
- 线上健康检查：`https://api.zero007.chat/health` 返回 `{"status":"ok"}`。
