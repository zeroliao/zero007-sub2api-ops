版本：002
状态：已提测
类型：混合
是否包含 upstream 同步：否
触发人：用户

涉及仓库：
- sub2api-src：有改动，分支 dev/002
- sub2api：有改动，分支 dev/002

sub2api-src commit：
- 初始 main commit：614f0b39bd463b0188d5e884eb307eb6c1cb4102
- dev/002 commit：4612d01b8402d00eae0667f3ec7e8b924bb39246

sub2api commit：
- 初始 main commit：89f527ff5fc806adf95303785c632d72e8f127b0
- dev/002 compose commit：eb8f5a0f9d45d262e4830a239c42cab88e932a26

镜像 digest：ghcr.io/zeroliao/sub2api@sha256:4f283d59417c9aa97cdf39ca41aa89302f59918aad5416600ddd9a02a9b29945
compose commit：eb8f5a0f9d45d262e4830a239c42cab88e932a26

本地 Docker 验证：
- 源码开发阶段验证通过：
  - `docker run --rm -v ${PWD}\backend:/app/backend -w /app/backend golang:1.26.3-alpine sh -c "go test ./internal/service -run 'Test(AccountTestService_OpenAIImageOAuthHandlesOutputItemDoneFallback|AccountTestService_TestAccountConnection_OpenAI|OpenAIGatewayService_|OpenAI)' -count=1"`
  - `docker run --rm -v ${PWD}\backend:/app/backend -w /app/backend golang:1.26.3-alpine sh -c "go test ./internal/service ./internal/handler -count=1"`
  - `docker build -t sub2api-verify:local .`
  - `GET http://127.0.0.1:18080/health` 返回 `200 {"status":"ok"}`
- release digest 验证通过：
  - 验证机器：Windows / Docker Desktop。
  - `docker compose -f deploy/docker-compose.yml config --quiet` 通过。
  - 候选应用镜像 `ghcr.io/zeroliao/sub2api@sha256:4f283d59417c9aa97cdf39ca41aa89302f59918aad5416600ddd9a02a9b29945` 拉取成功。
  - 本地临时验证 compose 启动 PostgreSQL、Redis、Sub2API、Caddy，均为 healthy。
  - `http://127.0.0.1:39080/health` 返回 `200 {"status":"ok"}`。
  - 健康后最近 30 秒应用日志无 fatal / panic / error / failed。
  - 追加修复镜像本地验证同样通过：`http://127.0.0.1:39080/health` 返回 `200 {"status":"ok"}`，健康后日志无 fatal / panic / error / failed。
  - 说明：本机 Docker Hub mirror 对 `postgres` / `redis` fixed digest referrers 返回 HTML，临时验证 compose 使用 `registry-1.docker.io/library/...@sha256:<digest>` 拉取同一 digest 后通过；生产 compose digest 未改变。临时验证环境需 `docker compose down -v` 清理本地匿名 PostgreSQL volume，避免复用旧本地密码。

服务器验证：
- 发版前 `diff-server`：通过，本地 `deploy/docker-compose.yml` 与服务器 live compose 无差异。
- validate-candidate：待执行。
- 备份：待执行。
- 蓝绿部署：待执行。
- 活跃槽位：待确认。

部署结果：待执行
回滚目标：v001 或当前生产槽位

版本内容：
- 修复 OpenAI OAuth 图像测试路径使用普通 `HTTPUpstream.Do` 导致代理/TLS 处理不一致的问题。
- OpenAI 账号测试、Responses、chat completions、messages、images、usage snapshot 等 HTTP 请求路径统一收敛到 `DoWithTLS`。
- 为 OpenAI 网关和账号用量服务补充 TLS profile 注入与 nil-safe helper。
- 增加 OpenAI 连通性排查交接文档，记录覆盖范围、验证命令和剩余风险。
- 运维版本流程文档补充发版复盘固化规则。

备注：
- 真实 OpenAI OAuth/API Key 连通性仍依赖有效账号、token/API key、代理和上游状态；本版本已完成代码路径、单测、构建和本地启动验证。
- 源码 `release/002` 已推送，候选镜像 tag `ghcr.io/zeroliao/sub2api:release-002-8e07904` 已发布，registry digest 如上。
- 生产真实测试在首个 002 候选镜像上仍复现 OpenAI OAuth 账号测试代理 TLS 错误，已追加修复并重新构建候选镜像 tag `ghcr.io/zeroliao/sub2api:release-002-4612d01`，registry digest 已更新为当前记录值。
