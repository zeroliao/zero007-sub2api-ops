# 版本：028

状态：开发中
类型：上游同步 / 源码
是否包含 upstream 同步：是
触发人：用户明确要求提交并部署

## 涉及仓库

- `sub2api`：是，包含订阅源扫描超时修复、此前用户要求的 upstream 同步及其前后端编译适配。
- `zero007-sub2api-ops`：是，记录本版本候选 compose、验证和生产部署结果。

## 初始状态

- `sub2api` 初始 commit：`681a8bff90e36735f3f57ee21feec3411912bf7a`
- `zero007-sub2api-ops` 初始 commit：`782c4ed9cc7bac9d9cb0cf8fd32fe086e4b3d39f`
- 上一成功版本：`v027`
- 新建分支：
  - `sub2api/dev/028`
  - `zero007-sub2api-ops/dev/028`

## 本版本目标

修复订阅源扫描请求因 Axios 30 秒超时而被前端提示 `Network error` 的问题，并将此前用户主动要求的 upstream 同步及必要的前后端适配纳入可追溯的源码候选版本。

此前 upstream 合并提交已经直接存在于源码 `main`，不重写历史；本版本从当前源码 `main` 创建开发线并记录该事实。生产候选仍只从 `release/028` 构建。

## 开发验证记录

- 前端 `vue-tsc --noEmit`：通过。
- 前端 API Vitest：21 passed。
- 相关前端 ESLint 检查：通过。
- 后端 `go test ./internal/service ./internal/handler`：通过。

## 候选与部署记录

- 源码 release commit：待生成。
- 候选镜像 digest：待 `GHCR Image` workflow 成功后填写。
- 运维 compose commit：待候选 digest 写入后生成。
- 本地 Docker exact-digest 验证：待执行。
- 服务器候选验证：待执行。
- 部署结果：待执行。
- 备份位置：待执行。
- 部署 run：待执行。

## 回滚与风险

- 回滚目标：`v027`，线上已验证镜像 digest 见 `docs/releases/027.md`。
- 已知风险：源码 `main` 在本地包含尚未推送到 `origin/main` 的上游及适配提交；本版本将完整提交链推送到 `dev/028` / `release/028`，部署前必须以同一 release commit、ops commit 和 immutable digest 完成验证。
- 未验证项：本地 Docker、服务器候选、生产健康检查、日志、活跃槽位和核心路径，均待后续门禁执行。

## Changelog

- Changed：延长订阅源扫描请求超时，避免正常慢响应被误报为网络错误。
- Changed：同步 upstream 后补齐前后端编译适配，保持现有 fork 行为。
