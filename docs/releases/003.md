版本：003
状态：成功
类型：运维
是否包含 upstream 同步：否
触发人：用户

涉及仓库：
- sub2api-src：不改动，记录当前 main commit。
- sub2api：有改动，分支 dev/003。

sub2api-src commit：
- main commit：275996000795bebc479ab1cbd0b82a7cd6c0cb5a

sub2api commit：
- 初始 main commit：b77b9d3
- release/003 commit：见 `release/003` HEAD / `v003` tag

版本内容：
- 新增独立 `clash-node` 服务，使用 `sing-box` 暴露 Shadowsocks 入站，供 Clash Verge 手动添加节点使用。
- 节点密码由服务器 `.env` 的 `CLASH_NODE_PASSWORD` 提供，不写入 Git。
- 默认端口 `8388`，同时发布 TCP/UDP。
- 保留原有 `sing-box` sidecar，不改动内部代理分发链路。
- 蓝绿部署脚本补充 sidecar 管理逻辑，后续部署会自动确保 `sing-box` / `clash-node` 启动并通过健康检查。

验证结果：
- `docker compose -f deploy/docker-compose.yml config --quiet`
- 生产同版本 `sing-box check` 校验 Shadowsocks 入站配置。
- 服务器 `validate-candidate` 通过，候选 commit `3501bfe6e32f8cde6b5c6f627363700ac4a4fc14`。
- 蓝绿部署成功，run id `20260601T000613Z-bluegreen-deploy-1620775`，完成时间 `2026-06-01T00:06:52Z`。
- 手动强制重建 `clash-node` 后确认容器 `healthy`。
- 追加修复 `dbe93b7a6206f9ee1bb70fc5988d5cec46a4a3e9` 已补充蓝绿部署自动管理 sidecar 逻辑。
- 追加修复 `94eb5e6` 固定 shell 脚本 LF 换行，避免 Windows 工作区 CRLF 导致服务器 `bash` 解析失败。
- 最终蓝绿部署成功，run id `20260601T011504Z-bluegreen-deploy-1662902`，完成时间 `2026-06-01T01:15:44Z`。
- 服务器监听：
  - `0.0.0.0:8388/tcp`
  - `0.0.0.0:8388/udp`
- 本机直连服务器真实 IP `43.133.231.45:8388/tcp` 测试通过。
- Sub2API 健康检查 `https://api.zero007.chat/health` 返回 `200 {"status":"ok"}`。

部署结果：成功
回滚目标：v002 或当前生产槽位
