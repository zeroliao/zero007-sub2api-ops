# v015 全局版本号规则修正

## 范围

- 状态：开发中
- 类型：混合
- 是否包含 upstream 同步：否
- 触发人：用户指出版本号必须是两个仓库共享的全局序列
- 源码分支：`sub2api/dev/015`
- ops 分支：`zero007-sub2api-ops/dev/015`
- 源码提交：`3475d86879a7c501249f229d3d3c817fc996ed48`
- ops 提交：`c72a259e927aab02175fb116fbc7e334255c46cd`
- 候选镜像 digest：`ghcr.io/zeroliao/sub2api@sha256:3146f8f0236a3fb28efd5dc02642bdd11aa5c5be876e93a4649693c1d48f5435`
- 回滚目标：`v014`

## 变更

- 明确版本号是 `sub2api` 和 `zero007-sub2api-ops` 共享的全局单调递增序列，不按仓库分别计数。
- 明确任一仓库的本地/远程 `dev/*`、`release/*`、`v*` tag，或运维仓库 `docs/releases/*.md` 使用过版本号后，该版本号即视为已占用。
- 明确下一次创建版本必须使用全局最大已占用版本号 + 1；某个仓库未参与旧版本时，后续也不能补用该旧版本号。
- 明确 `docs/releases/<version>.md` 是全局版本记录，文件名必须与版本号一致，即使只改源码仓库也必须创建。
- 同步更新 ops/source 快捷发布流程、两个仓库 `AGENTS.md` 和源码开发指南中的版本创建提醒。
- 将 ops compose 基线同步到服务器当前已验证镜像 digest，避免本次部署把生产回退到 v014 初始候选镜像。

## 验证

- `git -C zero007-sub2api-ops diff --check` 已通过，仅有 Windows 换行提示。
- `git -C sub2api diff --check -- AGENTS.md DEV_GUIDE.md` 已通过，仅有 Windows 换行提示。
- 关键字搜索已确认规则同步到 `docs/version-management.md`、`docs/source-release-flow.md`、两个仓库 `AGENTS.md` 和源码开发指南。
- `diff-server` 发现本地 ops compose 仍为 v014 初始 digest，服务器已运行 v014 追加部署确认的 `3146f8f0...` digest；本版本已纳入基线修正。

## 部署记录

- 不涉及生产部署。
