# v018 代理订阅源策略保存闭环

## 范围

- 状态：成功
- 类型：源码
- 是否包含 upstream 同步：否
- 触发人：用户要求部署和提交代码
- 源码分支：`sub2api/release/018`
- ops 分支：`zero007-sub2api-ops/release/018`
- 源码提交：`936f267414163fa5dbf90d47d444dc4292bb4be0`
- ops 提交：`5445bbb1f855f8bb524951de48ab6125c3362960`
- 候选镜像 digest：`ghcr.io/zeroliao/sub2api@sha256:a8d48a4925d5c5fd48070fc85ef945862ff9dc07672d5c90dfd55c02fdd6a0ce`
- 回滚目标：`v017`

## 变更

- 代理订阅源弹窗支持新增/编辑双模式。
- 订阅源列表新增“编辑”操作，可将已有订阅源配置和完整策略指标回填到表单。
- 已有订阅源可直接保存名称、URL、sidecar、端口、扫描周期、纯净度配置和策略指标。
- 修复“订阅策略指标编辑后没有明确保存入口”的交互闭环问题。
- 将 Go 工具链与容器构建基线从 `1.26.3` 升级到 `1.26.4`，修复标准库安全扫描告警。
- 将 `golang.org/x/net` 升级到 `v0.55.0`，并同步相关 `golang.org/x/*` 依赖。
- 补充 `js-cookie` 传递依赖审计例外说明；当前项目代码不直接使用该包处理 Cookie。
- 修复 `wire_gen_test.go` 的清理服务测试参数，适配当前 `provideCleanup` 签名。
- 修复 Go lint 问题：响应体关闭检查、Go 格式化、嵌入字段选择器、代理延迟缓存 nil 检查顺序，以及 h2c deprecation 抑制。

## 验证

- 前端类型检查：`pnpm --dir frontend typecheck` 已通过。
- 前端构建：`pnpm --dir frontend build` 已通过。
- 前端 lint：`pnpm --dir frontend lint:check` 已通过。
- 后端 targeted tests 已通过：
  - `TestFetchProxySubscription_InvalidURLReturnsBadRequest`
  - `TestParseProxyLine_AnyTLSIsMarkedAsSidecar`
  - `TestSidecarProxyHostDefaultsToComposeService`
  - `TestResolveOpenAIForwardModel`
  - `TestResolveOpenAICompactForwardModel`
  - `TestFetchProxySubscription_SendsBrowserLikeHeaders`
- Go 版本与依赖安全修复验证：
  - 本机 Go：`go1.26.4 windows/amd64`
  - `go test ./cmd/server ./internal/service -count=1` 已通过。
  - `go test ./cmd/server ./internal/server -count=1` 已通过。
  - `go vet ./cmd/server ./internal/service ./internal/handler/admin ./internal/server` 已通过。
- GitHub Actions：
  - `Security Scan` 通过，run `27223682411`。
  - `CI` 通过，run `27223682037`。
  - `GHCR Image` 通过，run `27223682729`。

## 部署记录

- GHCR 候选镜像：`ghcr.io/zeroliao/sub2api@sha256:a8d48a4925d5c5fd48070fc85ef945862ff9dc07672d5c90dfd55c02fdd6a0ce`。
- 本地 Docker 验证：Docker Desktop 可用；`docker compose -f deploy/docker-compose.yml config` 因缺少本地生产 `.env` 必需变量阻断，未使用假密钥绕过门禁。
- `diff-server`：通过，候选 compose 与服务器 live compose 仅应用镜像 digest 不同，`sing-box`、`clash-node`、`vpn-ws-node` sidecar 均保留。
- 服务器验证：`validate-candidate` 已通过，服务器检出 ops commit `5445bbb1f855f8bb524951de48ab6125c3362960`，候选 compose validation passed。
- 显式备份：`/opt/sub2api-deploy/backups/20260609T173911Z`。
- 蓝绿部署内备份：`/opt/sub2api-deploy/backups/20260609T173956Z`。
- 部署结果：蓝绿部署成功，run id `20260609T173956Z-bluegreen-deploy-3367517`，`exit_code=0`，完成时间 `2026-06-09T17:40:46Z`，活跃槽位切换为 `blue`。
- 部署后验证：`status` 显示 `sub2api-blue` 使用候选 digest 且 healthy，`https://api.zero007.chat/health` 健康检查通过。
