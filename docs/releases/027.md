# 版本：027

状态：开发中
类型：CI 修复
是否包含 upstream 同步：否
触发人：Codex 发布门禁

## 涉及仓库

- sub2api：是，删除 upstream 同步后失去调用方的 fork 死代码
- zero007-sub2api-ops：是，仅记录版本流程

## 初始状态

- sub2api 初始 commit：`a15496fe9dbbd8baebac010c2082f69ddc6938c3`
- zero007-sub2api-ops 初始 commit：`3a522574f2ddb027de4aa294a7ccdef2f671c0c8`
- 上一成功版本：`v026`
- 新建分支：
  - `sub2api/dev/027`
  - `zero007-sub2api-ops/dev/027`

## 本版本目标

修复 `v026` tag CI 中 `golangci-lint` 报告的未使用函数。`resolveRuntimeProxyURLAllowCustomRelay` 是 fork 既有辅助函数；其调用方在 026 的 upstream 同步后已消失，upstream 不包含该文件。删除该死代码，不改变生产请求转发行为。

## 验证记录

- 使用官方 `golangci/golangci-lint:v2.9-alpine` 执行 CI 同款 `golangci-lint run --timeout=30m`：通过，`0 issues`。
- `GOPROXY=https://goproxy.cn,direct go build ./internal/service ./cmd/server`：通过。
- 本次仅删除无调用方的内部函数，仓库中不存在对应 targeted test；候选镜像构建后执行本地 exact-digest Docker 验证。

## 当前结果

- 死代码已删除，CI 同款 lint 和生产包 build 已通过；待提交并构建候选镜像。
- 回滚目标：`v026` 及 digest `sha256:a590b29383d520609f9afdefe6714811908520d8f50ba3848fd5e4deed28b209`。
