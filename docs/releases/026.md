# 版本：026

状态：开发中
类型：上游同步
是否包含 upstream 同步：是
触发人：用户

## 涉及仓库

- sub2api：是，将 upstream 最新代码同步到 `dev/026`，并修复同步后仍存在的问题
- zero007-sub2api-ops：是，仅记录版本流程

## 初始状态

- sub2api 初始 commit：`018055b8e0bab66ade3803114615657d0bf5508f`
- zero007-sub2api-ops 初始 commit：`37258d582594b769ae2cb5b82aabda661a6b94e7`
- 上一成功版本：`v025`
- 新建分支：
  - `sub2api/dev/026`
  - `zero007-sub2api-ops/dev/026`

## 本版本目标

将 `upstream/main` 从 `b1a6b8026764dedf6a5f76d02bdc292468d22753` 同步到 `63cef6059`（upstream `0.1.163`）。非冲突 upstream 代码保持原样，只处理实际合并冲突，并修复同步后仍可复现的 CI 问题。

## 上游同步记录

- upstream remote：`https://github.com/Wei-Shaw/sub2api.git`
- 同步前 upstream/main commit：`b1a6b8026764dedf6a5f76d02bdc292468d22753`（`0.1.160`）
- 目标 upstream/main commit：`63cef6059`（`0.1.163`）
- Source 合并 commit：`a15496fe9`

## 冲突处理

- `README.md`：采用 upstream logo，保留 fork 的 `AGENTS.md` 协作入口，并按当前 `go.mod` 将 Go badge 更新为 `1.26.5`。
- `README_CN.md`：采用 upstream logo 和新增赞助内容，保留中文文档结构，并按当前 `go.mod` 将 Go badge 更新为 `1.26.5`。
- `backend/internal/service/openai_gateway_messages.go`：保留 upstream 的 Grok encrypted reasoning strip-and-retry 逻辑；发送请求继续使用 fork 的 `DoWithTLS` 和账号 TLS profile。
- `backend/internal/service/wire.go`：保留 fork 的 `ProvideOpenAIGatewayService` TLS 注入，同时加入 upstream 的 `ProvideImageStorageSettingService`。
- `deploy/README.md`：保留中文表格，合入 upstream 的 `EDGE_SECURITY.md` 和 `UPDATE_GITHUB_TOKEN` 说明。

## 同步后修复

- `account_handler_available_models_test.go`：为 `NewAccountTestService` 补齐新增的 proxy health 与 TLS profile 依赖占位。
- `wire_gen_test.go`：删除当前 `wire_gen.go` 的 `provideCleanup` 签名中不存在的 `proxySubscriptionScanRunner` 占位。
- Grok credential failover 与 OpenAI image failover 测试 mock：显式实现 `DoWithTLS` 并转调现有 `Do`，避免 nil embedded interface 导致测试 handler 返回 500；不修改生产 failover 逻辑。

## 验证记录

- Go toolchain：`1.26.5`；`GOPROXY=https://goproxy.cn,direct`。
- `go build ./internal/service ./cmd/server`：通过。
- `go test ./internal/handler/admin ./cmd/server -count=1`：通过。
- Grok credential failover 与 OpenAI image failover targeted unit tests：通过。
- Grok messages / encrypted-content retry targeted service unit tests：通过。
- `go test ./internal/handler ./internal/repository ./internal/service -count=1`：`handler`、`repository` 通过；`service` 仅已知时序敏感用例 `TestContentModerationRuntimeSnapshotRefreshFailureKeepsStaleConfig` 偶发失败，单独复跑通过。
- `pnpm.cmd --dir frontend run typecheck`：通过。
- `pnpm.cmd --dir frontend run build`：通过；仅有既有 dynamic import、chunk size、Browserslist 提示。
- Docker daemon：已启动；候选 exact digest 验证待 release 镜像构建后执行。

## 当前结果

- upstream `63cef6059` 已合入 Source `dev/026`，Source commit 为 `a15496fe9`。
- 5 个合并冲突已按 upstream 架构优先和 fork TLS 能力保留原则解决。
- 4 个同步后仍存在的测试适配问题已修复，代码级验证已完成。
- dev 分支推送、release 候选镜像、本地 Docker 验证和服务器部署待完成。
