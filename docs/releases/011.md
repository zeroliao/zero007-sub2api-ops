# v011 优化账号模型同步与自动审查模型

## 范围

- 版本类型：源码。
- 源码分支：`sub2api/release/011`。
- ops 分支：`zero007-sub2api-ops/release/011`。
- 源码提交：`8574c65cca1a76dc967fcab8b9aa78a23efd141f`。
- 候选镜像 digest：`ghcr.io/zeroliao/sub2api@sha256:9e9d6e1e046da92e3c90c1e68ba74a8aa4ab8749f0668a4c9d43da17126d92c4`。
- 回滚目标：`v010` / 线上当前 digest `ghcr.io/zeroliao/sub2api@sha256:87fe9d9aa433474fc1428e4acc9f80a8554d531566eb0d9eebaa4da423c611fc`。

## 变更

- 修复 `codex-auto-review` 上游模型映射，统一转发到 `gpt-5.5`，避免自动审查请求调用旧模型导致失败。
- 新增 API Key 账号模型预览接口，创建账号时可使用当前输入的 API Key 从 OpenAI、Anthropic、Gemini 上游拉取可用模型。
- 优化账号创建页模型白名单交互：API Key 账号显示“获取当前 Key 可用模型”，OAuth 账号显示“填入推荐模型”。
- 更新 OpenAI OAuth 推荐模型列表，只保留当前建议使用的 GPT/Codex/Image 模型，避免把不可用模型误加入白名单。
- 保持不支持 embeddings 模型，本次不新增 `/v1/embeddings` 能力。

## 验证

- 后端目标测试：已通过，覆盖 API Key 模型预览与账号可用模型接口。
- 后端模型映射测试：已通过，覆盖 `codex-auto-review -> gpt-5.5`。
- 前端目标测试：已通过，覆盖模型白名单与账号测试弹窗。
- 前端 typecheck：已通过。
- 前端 build：已通过。
- `git diff --check`：已通过。
- 候选镜像 digest：已通过 GHCR tag `release-011-8574c65` 解析到 immutable digest。
- 本地 Docker 验证：待执行。
- `diff-server`：待执行。
- `diff-server`：通过，服务器当前 compose 与候选 compose 仅应用镜像 digest 不同，`sing-box` 和 `clash-node` sidecar 保持一致。
- `validate-candidate`：通过，服务器检出 ops commit `db53a020855317c940d8afa0096f17c24dd6b14f`，candidate compose validation passed。
- 发布前备份：完成，备份目录 `/opt/sub2api-deploy/backups/20260602T154729Z`。
- 蓝绿部署：待执行。
- 线上健康检查：待执行。

## 结果

- 状态：开发中。
- compose commit：`db53a020855317c940d8afa0096f17c24dd6b14f`。
- 部署 run id：待填写。
- 生产结果：待填写。
