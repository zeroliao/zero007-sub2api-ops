# Version 035

Status: 成功
Type: source / ops deployment
Triggered by: explicit user request

## Scope

- 修复订阅源按 Chrome `User-Agent` 返回 HTML 下载页时被错误解析为无效代理节点的问题。
- 订阅请求使用专用客户端 `User-Agent` 和代理配置优先的 `Accept`，并拒绝 HTML 响应。
- 增加订阅抓取与 Clash YAML 解析回归测试。

## Candidate

- Source development baseline: `dev/035 @ 8cfae296a527e9ff065eb3a4cbf9c31e6ffc8b3f`
- Source branch: `release/035 @ 8cfae296a527e9ff065eb3a4cbf9c31e6ffc8b3f`
- Candidate image tag: `ghcr.io/zeroliao/sub2api:release-035-8cfae29`
- Sub2API image digest: `ghcr.io/zeroliao/sub2api@sha256:0c064e79107d6e74699b9085dfcbe6448cf71adffd1b444802879203f1dfbcc9`
- Ops development commit: `dev/035 @ 02d23cae42abce676c28d04168e8a6d08189b488`
- Ops release commit: `release/035 @ 02d23cae42abce676c28d04168e8a6d08189b488`
- Rollback target: successful version `v034` / `ghcr.io/zeroliao/sub2api@sha256:f5b7ed2082ec4bb55bf87654a299332b2639b52e711100c2b688abba2e5c99e9`

## Validation

- Backend targeted subscription tests: passed.
- Backend all-package compile check: passed.
- Real subscription URL comparison: Chrome UA returned HTML; `Sub2API/1.0` returned Clash YAML with 89 proxies.
- Local Docker exact-digest validation: pending; Docker Desktop Linux daemon unavailable.
- Server candidate validation: passed; candidate compose validation completed on the server.
- Backup: `/opt/sub2api-deploy/backups/20260830T063246Z`.
- Deployment: successful via foreground `bluegreen-deploy` at `2026-08-30T06:32:46Z`; active slot switched to `blue`.
- Deployment verification: `sub2api-blue` healthy with the target digest, old `sub2api-green` stopped, required sidecars healthy/running, no fatal/panic/database/connection-refused/migration-failed patterns in recent application logs, and `https://api.zero007.chat/health` returned `{"status":"ok"}`.
- The initial background run `20260830T063049Z-bluegreen-deploy-719114` exited 255 during image pull/session interruption before candidate startup; it did not switch traffic. The foreground retry completed successfully.
- CI risk: source CI failed on pre-existing issues outside this version's diff (`proxy_repo_exists_test.go` errcheck, unused `isLocalTCPPortReachable`, existing `SettingsView.spec.ts` failures, and `openai_images_failover_test.go` compile error). Subscription targeted tests and candidate image build passed.

## Checklist

- [x] Source fix committed and pushed to `dev/035` and `release/035`
- [x] GHCR candidate image built successfully and immutable digest recorded
- [x] Candidate digest pinned in `deploy/docker-compose.yml`
- [x] Pre-deployment `diff-server` completed; server compose matched v034 baseline
- [x] Ops `dev/035` and `release/035` pushed
- [ ] Local Docker exact-digest validation
- [x] `validate-candidate`
- [x] Backup
- [x] Blue-green deployment
- [x] Target container health, logs and public health endpoint verified
- [ ] Subscription path verified through authenticated admin API
- [x] Release merged to `main` and `v035` tags created (`sub2api` source `v035` and ops `v035` pushed)
