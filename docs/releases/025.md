# 版本：025

状态：开发中
类型：上游同步
是否包含 upstream 同步：是
触发人：用户

## 涉及仓库

- sub2api：是，将 upstream 最新代码同步到 `dev/025`
- zero007-sub2api-ops：是，仅记录版本流程

## 初始状态

- sub2api 初始 commit：`bcab8dc649aca0af073039cadf6c98392964e65a`
- zero007-sub2api-ops 初始 commit：`ff87a9969ad08916d9c88ad49f457423f0e2ce22`
- 上一成功版本：`v024`
- 新建分支：
  - `sub2api/dev/025`
  - `zero007-sub2api-ops/dev/025`

## 本版本目标

拉取 `https://github.com/Wei-Shaw/sub2api.git` 最新代码，并把同步结果纳入 `sub2api/dev/025`，不直接同步到 `main`。

## 上游同步记录

- upstream remote：`https://github.com/Wei-Shaw/sub2api.git`
- 同步前 upstream/main commit：`4a5665da5b2c6b83c4597844ea6e573746c821b1`（`0.1.137`）
- 目标 upstream/main commit：`b1a6b8026764dedf6a5f76d02bdc292468d22753`（`0.1.160`）
- 共同基线：`4a5665da5b2c6b83c4597844ea6e573746c821b1`
- 分支差异：fork 侧 38 个 commit，upstream 侧 997 个 commit
- 文件差异：1424 files changed，275768 insertions，63128 deletions

## 冲突处理摘要

- 保留 upstream 拆分后的 Gateway/OpenAI/Antigravity service 结构、billing probe、Grok 和 security audit 能力。
- 迁移 fork 专属的代理分发、订阅扫描、节点绑定历史、AbuseIPDB 设置、代理删除解绑和 API Key 模型预览能力。
- 合并 admin routes 的 step-up auth、upstream billing probe、模型预览和代理分发/订阅路由。
- AccountTestModal 使用 upstream 的账号可用模型 API，同时保留 fork 的 OpenAI 稳定模型默认选择；i18n 本地 key 已迁移到 upstream 拆分后的 locale 文件。
- 移除 fork 的 `codex-auto-review -> gpt-5.5` 映射，恢复 upstream 对 `codex-auto-review` 的独立模型透传语义；通过 Wire provider 补齐 fork 的 OpenAI TLS profile 注入。
- 已清除全部合并冲突标记；Git 状态为 `All conflicts fixed but you are still merging`。

## 验证记录

- Go toolchain：`1.26.5`，下载源使用 `https://goproxy.cn,direct`。
- `go build ./internal/service`：通过。
- `go build ./cmd/server`：通过。
- 移除 `codex-auto-review -> gpt-5.5` 映射后重新运行 `go build ./internal/service`：通过；恢复 OpenAI TLS test helper 的最小观测字段后，模型映射与 OpenAI image targeted tests 通过。
- `pnpm.cmd --dir frontend run typecheck`：通过。
- `pnpm.cmd --dir frontend run build`：通过；Vite 仅输出既有 chunk-size 和 dynamic-import warnings。
- 冲突相关 Vitest：5 个 test files、33 个 tests 全部通过（AccountTestModal、AppSidebar、useModelWhitelist、locale key collision）。
- `go test ./internal/service ./internal/handler ./internal/repository -count=1`：`internal/handler`、`internal/repository` 通过；已修复 `recordAccountProxyBinding` 等 fork proxy history helper 在测试 service 未注入 `entClient` 时的 nil safety，并移除 handler test 对已删除 TLS profile 构造参数的旧调用。修复后的针对性用例通过；组合运行时仅剩已有的 `TestContentModerationRuntimeSnapshotRefreshFailureKeepsStaleConfig` 偶发 1 秒时序失败，该用例单独重跑通过，未扩大范围修改无关 content moderation 逻辑。
- `go build -tags embed` 交叉编译 Linux backend binary：通过。
- 隔离本地 runtime 验证：使用当前源码 Linux binary、独立 PostgreSQL/Redis volumes 和本地缓存 runtime image；三个容器均 healthy，`GET /health` 返回 200、`GET /` 返回前端 HTML、`GET /setup/status` 返回 `needs_setup=false`，未授权接口返回 401，错误密码登录返回 401；验证栈已清理。
- 完整 `docker-compose.dev.yml` 源码镜像构建：未完成。首次因 Dockerfile frontend layer size validation 失败，显式拉取 `docker/dockerfile:1.7` 后恢复；随后基础镜像依赖下载长时间无输出，已终止，未影响其他 Docker 项目。
- `go generate ./cmd/server`：未通过，原因是 upstream 已有的 Wire `PromptAdminService` provider 缺口；本次仅对现有 `wire_gen.go` 做冲突所需的针对性调用适配。
- 未运行全量 upstream tests，避免将已验证的非冲突代码扩大为本地修复范围。

## 发布与部署记录

- 未创建 commit、未 push、未创建 `release/025`、未部署。
- 本地 Docker 验证尚未进行，需在后续 release 节点按版本管理流程执行。

## 当前结果

- 已创建两个仓库的 `dev/025` 分支。
- 目标 upstream commit `b1a6b8026764dedf6a5f76d02bdc292468d22753` 已纳入 `sub2api/dev/025` 合并工作区。
- 冲突已处理，生产后端 build、前端 typecheck 和冲突相关前端 tests 已通过。
- 已根据本次同步复盘在 `docs/version-management.md` 固化 upstream 同步执行准则，包括国内镜像源、冲突范围收敛、upstream 语义优先和分层验证顺序。
- 当前仍处于未 commit 的 merge 状态，等待用户后续确认提交/发布流程。
