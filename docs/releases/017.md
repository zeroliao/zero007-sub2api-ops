# v017 请求链路观测与代理订阅交互恢复

## 范围

- 状态：部署中
- 类型：源码
- 是否包含 upstream 同步：否
- 触发人：用户要求增加秒级时间范围查询，并按规划任务执行后直接部署到服务器
- 源码分支：`sub2api/release/017`
- ops 分支：`zero007-sub2api-ops/release/017`
- 源码提交：`ba5e70987fb702a3275f616a4a21309746fc3b2e`
- ops 提交：待部署完成回填
- 候选镜像 digest：`ghcr.io/zeroliao/sub2api@sha256:46571a36f70a14521e1f3436a98759c2f60466690f34a391bc116ab3d7642655`
- 回滚目标：`v016`

## 变更

- 恢复 v014 中代理订阅源节点筛选与策略预览交互，补回订阅节点列表路由、节点详情返回入口、筛选/排序/预设、策略保存和保存后扫描。
- 请求明细接口扩展链路字段，统一展示成功请求与错误请求的用户、API Key、账号、分组、endpoint、模型映射、请求类型、TTFT、上游延迟、响应延迟、错误 owner/source/phase。
- 请求明细搜索扩展到用户邮箱、API Key 名称、账号名、分组名、endpoint、错误来源等字段。
- Ops 请求明细弹窗新增精确到秒的开始/结束时间范围，以及类型、排序、用户 ID、API Key ID、账号 ID、请求 ID、模型和关键词筛选。
- Ops 请求明细表格新增用户/key、账号/分组、模型链路、endpoint 链路、质量指标和问题节点列，便于按账号、用户、密钥定位入站到上游再返回用户的全链路问题。

## 验证

- 前端类型检查：`pnpm typecheck` 已通过。
- 后端 repository ops 相关测试：`go test ./internal/repository -run 'Ops|RequestDetails|TimeRange' -count=1 -v` 已通过。
- 后端 admin handler ops 相关测试：`go test ./internal/handler/admin -run 'Ops|RequestDetails|TimeRange|parseOpsTimeRange' -count=1 -v` 已通过。
- 后端 service ops 相关测试：`go test ./internal/service -run 'Ops|RequestDetails' -count=1 -v` 已通过。
- `git diff --check` 已通过。
- GHCR 候选镜像：`ghcr.io/zeroliao/sub2api:release-017-ba5e709` 已发布，digest 已写入 ops compose。
- 服务器候选验证：待执行。
- 蓝绿部署：待执行。
- 线上健康检查：待执行。

## 部署记录

- 服务器验证：待执行。
- 备份：待执行。
- 部署结果：待执行。
