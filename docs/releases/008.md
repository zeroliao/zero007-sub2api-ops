# v008 手机端 Clash 订阅兼容

## 范围

- ops 版本：`release/008`。
- 源码版本：沿用线上应用镜像，本次仅调整运维订阅生成逻辑。

## 变更

- 保留现有完整 Clash 订阅：`/clash/<token>.yaml`。
- 新增手机端兼容订阅：`/clash/<token>.mobile.yaml`。
- 手机端兼容订阅移除 `dns`、`fake-ip`、`mixed-port` 等高兼容风险字段，仅保留 Shadowsocks 节点、基础分组和常用规则。
- 更新 `docs/clash-node.md`，补充手机端订阅 URL 查看方式。

## 验证

- `validate-candidate`：通过，候选提交 `09b6a8c0264842dd7f96451cb979df686ce6cdd6`。
- 发布前备份：完成，备份目录 `/opt/sub2api-deploy/backups/20260601T071917Z`。
- 蓝绿部署：通过，运行 `20260601T072006Z-bluegreen-deploy-1915978`，`exit_code=0`。
- 线上健康检查：`https://api.zero007.chat/health` 返回 `{"status":"ok"}`。
- 完整订阅：返回 `HTTP 200`，`Content-Type: text/yaml; charset=utf-8`。
- 手机端兼容订阅：返回 `HTTP 200`，`Content-Type: text/yaml; charset=utf-8`。
- 手机端兼容订阅确认不包含 `mixed-port` 和顶层 `dns` 配置，包含 `proxy-groups`。

## 注意

订阅 URL 已在截图中出现过，建议部署后轮换 `CLASH_SUBSCRIPTION_TOKEN` 和 `CLASH_NODE_PASSWORD`，再把新的手机端兼容订阅 URL 导入手机 Clash。
