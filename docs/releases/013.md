# v013 代理订阅资源退役与 sidecar 就绪闭环

## 范围

- 版本类型：源码
- 源码分支：`sub2api/release/013`
- ops 分支：`zero007-sub2api-ops/release/013`
- 源码提交：`0ba8680eb9350413eb7ab458bad3b2604aedb539`
- 候选镜像 digest：`ghcr.io/zeroliao/sub2api@sha256:8a9ee1d0de128e22b6b425b83939aed51a10a337230f1161d1368d31e1d770bb`
- 回滚目标：`v011` / 旧线上 digest `ghcr.io/zeroliao/sub2api@sha256:9e9d6e1e046da92e3c90c1e68ba74a8aa4ab8749f0668a4c9d43da17126d92c4`

## 变更

- 删除订阅源时同步退役关联订阅节点、sidecar endpoint、代理和账号绑定状态，避免后台删除后仍残留可分发代理。
- 订阅节点在最新扫描结果中缺失时，除了节点状态改为 `missing`，同时将对应 direct/sidecar 代理退役并把绑定标记为 `proxy_unavailable`。
- sidecar 节点入池后增加本地 `127.0.0.1:<port>` 可达性检查，只有端口真实就绪才将 sidecar endpoint 标记为 `ready` 并启用代理。
- 修复按地址查找代理时只看 active 记录的问题，避免 disabled 的 direct/sidecar 代理被重复创建。
- 补充 `isLocalTCPPortReachable` 单元测试，并修复发布过程中暴露出的编译问题。

## 验证

- 前端构建：`pnpm --dir sub2api/frontend build` 已通过。
- 源码候选镜像：`GHCR Image` workflow 已通过，run id `26892172934`。
- 候选验证：`validate-candidate` 已通过。
- 备份：`/opt/sub2api-deploy/backups/20260603T144920Z`
- 蓝绿部署：已通过，活跃槽位切换为 `green`。
- 线上健康检查：`https://api.zero007.chat/health` 返回 `{"status":"ok"}`。
- 线上容器状态：`sub2api-green` 使用新 digest 且为 `healthy`，`sing-box`、`clash-node`、`caddy`、`postgres`、`redis` 均保持运行。

## 结果

- 状态：成功
- compose 提交：`e09b63c03bff2e2dfe77bb3b392f257d9690659b`
- 生产结果：成功，当前活跃槽位 `green`
