# 版本：024

状态：已提测
类型：上游同步
是否包含 upstream 同步：是
触发人：用户

## 涉及仓库

- sub2api：是，已同步上游代码到 `dev/024`
- zero007-sub2api-ops：是，仅记录版本流程

## 初始状态

- sub2api 初始 commit：`801090cb3d322e4946dea2134a61d19e7ca248e7`
- zero007-sub2api-ops 初始 commit：`bd8ff4e64045ea65f7017d259363ab60d3267f86`
- 上一成功版本：`v023`
- 新建分支：
  - `sub2api/dev/024`
  - `zero007-sub2api-ops/dev/024`

## 本版本目标

拉取 `sub2api` fork 项目的上游仓库 `https://github.com/Wei-Shaw/sub2api.git` 最新代码，并把同步结果纳入 `sub2api/dev/024`，不直接同步到 `main`。

追加修复：处理代理订阅源全部删除后，后台“代理分发/订阅源”页面因后端空列表返回 `null` 而读取 `.length` 崩溃的问题。

## 上游同步记录

- upstream remote：`https://github.com/Wei-Shaw/sub2api.git`
- upstream/main commit：`4a5665da5b2c6b83c4597844ea6e573746c821b1`
- sub2api dev/024 merge commit：`32ec5e64cd88ebc21c6935d5593019c6a86c36cd`

## 冲突处理摘要

- 保留本地 `README_CN.md` 中文文档。
- 保留本地 `release.yml` 发布约定：GitHub Release/归档流程不重新构建生产镜像。
- H2C 相关实现采用上游 Go 1.26 `http.Protocols` 方案。
- 代理池相关模型合并本地账号代理分发字段与上游代理过期/fallback 字段。
- 删除代理时保留本地解绑账号与 `account_proxy_bindings` 标记 `proxy_unavailable` 的行为。
- `OpenAIGatewayService` 合并本地 TLS 指纹参数与上游 `UserPlatformQuotaRepository` 参数。
- OpenAI upstream 传输错误处理保留上游 failover 逻辑，同时保留本地 `DoWithTLS` 调用路径。
- 保留本地 `codex-auto-review -> gpt-5.5` 行为，并同步调整相关测试预期。
- 保留本地 API Key 模型预览与上游模型同步入口的组合交互。

## 验证记录

已通过：

- 后端：`go test ./internal/service -count=1`
- 后端：`go test ./internal/handler ./internal/handler/admin ./internal/server ./cmd/server -count=1`
- 前端：`pnpm.cmd --dir sub2api/frontend run typecheck`
- 前端：`node .\node_modules\vitest\vitest.mjs run --config .\vitest.config.ts src/components/account/__tests__/AccountTestModal.spec.ts`
- 前端追加修复：`pnpm.cmd --dir sub2api/frontend run typecheck`
- 本地 Docker 追加修复验证：使用当前源码构建 `docker-compose.dev.yml`，`http://127.0.0.1:8080/health` 返回 `200 {"status":"ok"}`。
- 页面追加修复验证：Playwright mock `GET /api/v1/admin/proxy-subscriptions` 返回 `null`，访问 `/admin/proxy-dispatch` 成功渲染，浏览器控制台 `Errors: 0`，未再出现 `Cannot read properties of null (reading 'length')`。

说明：

- Go 验证使用本地工作区缓存：`GOCACHE=C:\Users\Administrator\Desktop\code\sub2api-wrap\.cache\gocache`，`GOMODCACHE=C:\Users\Administrator\Desktop\code\sub2api-wrap\.cache\gomod`。
- 多个后端包一次性并行测试曾出现 `internal/service` 输出截断后的失败状态；随后 `internal/service` 单包复跑通过，其余目标包复跑通过。
- Vitest 在普通沙箱下加载配置会遇到路径访问限制，已使用提权命令验证通过。

## 部署记录

- sub2api release 分支：`release/024`
- sub2api release commit：`bcab8dc649aca0af073039cadf6c98392964e65a`
- GHCR Image workflow：`27782902640`
- 候选镜像 tag：`ghcr.io/zeroliao/sub2api:release-024-bcab8dc`
- 候选镜像 digest：`ghcr.io/zeroliao/sub2api@sha256:3dd71ab671a64cd0a6b467b6fd854e339d43b964b7e3aeeee0cb263da7e5621a`
- ops release 分支：`release/024`
- ops compose：已更新应用镜像 digest，保留现有 sing-box sidecar 与 Caddy 配置。
- 本地 fixed-digest Docker 验证：待执行。
- 服务器验证与部署：待执行。

## 当前结果

- `sub2api/dev/024` 已完成 upstream/main 合并并提交。
- `sub2api/dev/024` 已追加代理订阅源空列表兜底修复，并同步到 `release/024`。
- `zero007-sub2api-ops/dev/024` 记录本版本流程。
- `zero007-sub2api-ops/release/024` 已更新候选镜像 digest，后续继续执行本地 fixed-digest Docker 验证和服务器部署验证。
