# Version 036

Status: 开发中
Type: mixed source / ops deployment
Triggered by: explicit user request

## Scope

- 修复订阅源扫描把 HTML 下载页或“订阅已失效”占位节点当作真实节点的问题，避免错误标记旧节点并保留有效节点。
- 完善 Clash YAML 节点凭据、TLS、SNI、WebSocket、gRPC 和 Hysteria 参数转为标准 raw URI 的逻辑。
- 将订阅扫描状态改为数据库 lease/heartbeat，避免后台任务状态因进程内存或请求生命周期失真。
- 统一订阅 sidecar 代理名称和更新逻辑，修正 IP 管理页的代理 `disabled` 状态展示与筛选。
- 加强生产候选镜像 digest 校验和 SSH 远程运维命令的连接稳定性。

## Candidate

- Source development baseline: `dev/035 @ 8cfae296a527e9ff065eb3a4cbf9c31e6ffc8b3f`
- Source development commit: `dev/036 @ 5bfd61efeb3957c18d11eb2ab5ae20982d31ed43`
- Source release commit: `release/036 @ 5bfd61efeb3957c18d11eb2ab5ae20982d31ed43`
- Ops development baseline: `dev/035 @ ea35380`
- Ops development commit: `dev/036 @ e2e1b846efc8647a75b61dc10cf7446767fcad5b`
- Ops release commit: pending
- Sub2API image digest: `ghcr.io/zeroliao/sub2api@sha256:f02348c7d80fe1fbb9b38551ef7bcc043c790d03c3e0e03b56fd894ebe080d29`
- Compose commit: pending
- Rollback target: successful version `v035` / digest recorded in `docs/releases/035.md`

## Validation

- Backend subscription targeted tests: passed.
- Backend subscription scan lease unit tests: passed with `go test -tags unit ./internal/service -run 'TestProxySubscriptionScanJob|TestGetProxySubscriptionScanStatus|TestFinishProxySubscriptionScan' -count=1 -timeout=60s -v`.
- Backend migration test: passed.
- Backend all-package compile check: passed with `go test ./... -run '^$' -count=1 -timeout=120s`.
- Backend complete `internal/service` tests: passed with `go test ./internal/service -count=1 -timeout=150s`.
- Frontend `SettingsView` targeted tests: passed, 36 tests.
- Frontend typecheck: passed.
- Frontend lint: passed with two existing unused-variable warnings.
- Frontend production build: passed.
- Local Docker exact-digest validation: pending.
- Server candidate validation: pending.
- Subscription authenticated-path verification: pending; the configured source currently returns an expired-subscription response and must be renewed before real nodes can be checked.

## Checklist

- [x] Version number allocated from the shared sequence.
- [x] `dev/036` branches created in source and ops repositories.
- [x] Source and ops changes locally validated.
- [x] Source changes committed and pushed to `dev/036` and `release/036`.
- [x] GHCR candidate image built and immutable digest recorded.
- [x] Candidate digest pinned in production compose.
- [ ] Local Docker exact-digest validation.
- [ ] Ops candidate pushed and server `validate-candidate` completed.
- [ ] Production backup completed.
- [ ] Blue-green deployment completed.
- [ ] Health, logs, active slot, migration, and subscription path verified.
- [ ] Release merged to `main` and `v036` tags created in both repositories.

## Known Risks

- The current subscription URL is externally expired and cannot provide real nodes until the provider renews or replaces it. This blocks end-to-end validation of refreshed node credentials and the `31011` sidecar endpoint.
- Local Docker exact-digest validation depends on a working Docker daemon; if unavailable, record it as pending and retain server-side validation as a separate gate.
