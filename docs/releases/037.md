版本：037
状态：开发中
类型：混合
是否包含 upstream 同步：否
触发人：用户明确要求修复并部署
涉及仓库：源码、运维
sub2api-src commit：`release/037 @ ed056c0a453868052a7a97e61d6bd4787e202a6f`
sub2api commit：待提交候选 compose
镜像 digest：`ghcr.io/zeroliao/sub2api@sha256:914a825830f92efefb3c4aaf805f840833541d645735dd9ada1d4f11fc498c7b`
compose commit：待提交
本地 Docker 验证：pending，待候选镜像构建后使用同一 digest 验证
服务器验证：pending
部署结果：pending
回滚目标：成功版本 `v036` / `ghcr.io/zeroliao/sub2api@sha256:f02348c7d80fe1fbb9b38551ef7bcc043c790d03c3e0e03b56fd894ebe080d29`

## 变更范围

- IP 管理中的 `proxies` 由软删除改为物理删除，删除时清理 sidecar endpoint，并保留账号外键的既有解绑/级联语义。
- 订阅节点失效、订阅源扫描失败或 sidecar 探测失败时，物理删除订阅代理和 endpoint；订阅源与订阅节点仍保留软删除和扫描结果，用于诊断。
- 订阅节点按自身 `managed_proxy_id` 或 sidecar endpoint 复用代理，回收只允许删除 `source='subscription'` 的资源，避免同地址手工代理被误删。
- 远程 sidecar 对账清理历史软删除订阅代理、孤立 endpoint 和失效订阅资源，释放旧端口（包括 `31011`）。

## 数据影响与回滚

- 本版本没有新增或修改数据库 migration；删除 SQL 在订阅资源回收和 sidecar 对账运行时执行。
- 部署前执行 ops 仓库标准 PostgreSQL/config backup，备份路径写入本记录。
- 物理删除的代理、endpoint 和级联绑定不能通过应用回滚恢复；如需恢复，使用部署前 PostgreSQL dump 手工恢复，或回滚应用后重新导入订阅源。
- 用户已在部署前删除 IP 管理页面中的全部数据；部署后以数据库实际计数和 sidecar 对账结果为准。

## 验证

- Backend subscription targeted tests：通过。
- Backend repository hard-delete targeted test：通过。
- Backend all-package compile check：通过。
- Ops shell syntax check：通过。
- 完整 `internal/service` 测试：曾出现既有异步内容审计用例失败；该用例单独复跑通过，完整套件结果需在候选提交后重跑并记录。
