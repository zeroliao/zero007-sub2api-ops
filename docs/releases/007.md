# 007 发布记录

## 目标

进一步修复代理管理“快捷添加”对第三方 `socks5://...#备注` 链接的浏览器输入框校验问题。

## 变更

- 源码版本：`release/007`，提交 `3532696a`。
- 镜像 tag：`ghcr.io/zeroliao/sub2api:release-007-3532696`。
- 镜像 digest：`ghcr.io/zeroliao/sub2api@sha256:87fe9d9aa433474fc1428e4acc9f80a8554d531566eb0d9eebaa4da423c611fc`。
- 前端快捷添加不再依赖浏览器 `URL` 对 `socks5://` 的解析行为。
- 解析前先移除 `#备注`，再按代理 URL 语义解析协议、认证、host、port。

## 验收

- `pnpm.cmd --dir .\sub2api\frontend typecheck` 通过。
- 使用真实 Edge/Chromium textarea `input` 事件验证示例 `socks5://...#SOCKS-...`：`valid=1`、`invalid=0`。
- GHCR 候选镜像可拉取。
- `validate-candidate` 通过。
- 蓝绿部署成功。
- 线上 `/health` 返回 `200`。
