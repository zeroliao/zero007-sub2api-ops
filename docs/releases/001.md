版本：001
状态：已提测
类型：源码
是否包含 upstream 同步：否
触发人：用户

涉及仓库：
- sub2api-src：有改动，分支 dev/001
- sub2api：不改动，仅记录参与部署的当前 main commit

sub2api-src commit：
- 初始 main commit：29035acfbdac234d660921aa7d1b6c1adc8e86d7
- dev/001 commit：614f0b39bd463b0188d5e884eb307eb6c1cb4102

sub2api commit：
- main commit：6ec11438dce43b76d429ee451f41b45d15e61d6f

镜像 digest：ghcr.io/zeroliao/sub2api@sha256:14c5b5c626e87952a43cc9fca278aefd3adf1a4e7ad7e34a7da962a03572e8ee
compose commit：21bac1b45936cd9ff9b0511a4b3a7502fddc854c

本地 Docker 验证：
- 结果：通过。
- 验证机器：Windows / Docker Desktop 4.75.0 / Docker Engine 29.5.2。
- 已通过：`docker-compose -f deploy/docker-compose.yml config --quiet`。
- 已通过：候选应用镜像 `ghcr.io/zeroliao/sub2api@sha256:14c5b5c626e87952a43cc9fca278aefd3adf1a4e7ad7e34a7da962a03572e8ee` 拉取成功。
- 已通过：PostgreSQL、Redis、Sub2API、Caddy 容器启动，均为 healthy。
- 已通过：`http://127.0.0.1:39080/health` 返回 `200 {"status":"ok"}`。
- 已通过：最近 60 秒日志无 fatal / panic / error / failed。
- 说明：Docker daemon 配置的 Docker Hub registry mirrors 在拉取 `postgres` / `redis` 固定 digest 时返回 HTML，导致 `failed to decode referrers index: invalid character '<' looking for beginning of value`。本地验证使用显式 `registry-1.docker.io/library/...@sha256:...` 拉取同一 digest 后通过；生产 compose digest 未改变。

服务器验证：
- validate-candidate：通过，ops commit f5754fdcca7fe6be61914c007d5b524d13cbcb58。
- 备份：通过，路径 `/opt/sub2api-deploy/backups/20260531T122145Z`，时间 2026-05-31T12:21:45Z。

部署结果：待部署
回滚目标：上一个成功版本 tag 或当前生产槽位

已完成验证：
- 后端：docker run --rm -v "${PWD}:/src" -w /src/backend golang:1.26.3-alpine go test ./...
- 前端：pnpm --dir frontend run typecheck
- 前端构建：pnpm --dir frontend run build
- Diff 检查：git diff --check

版本内容：
- 将服务器现有 `sing-box` sidecar 纳入 release compose 基线，避免部署时移除。
- 新增账号代理分发表 account_proxy_bindings。
- 扩展 proxies 代理池元数据字段。
- 新增 proxy_subscription_sources 订阅源表。
- 新增账号身份识别、历史代理恢复、自动代理分发和账号删除同步。
- 默认 direct_fallback_mode=off，无可用代理时返回 503 NO_AVAILABLE_PROXY，不直连服务器 IP。
- 后台新增“代理分发”页面，支持查看账号代理关系、代理池、订阅源和策略。
- 支持代理导入预览/确认，识别直接代理、Clash YAML、sing-box JSON、订阅 URL 和 sidecar-only 节点。
- 主出站路径接入运行时代理策略。

备注：
- 源码 release/001 已推送，GHCR Image workflow run 26710761663 构建成功。
- 当前已写入候选镜像 digest，但本地 Docker 验证被 Docker Hub fixed digest 拉取错误阻塞；尚未完成服务器验证或部署。
