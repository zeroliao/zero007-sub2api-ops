# 006 发布记录

## 目标

修复代理管理“快捷添加”对第三方代理 URL 的前端校验过严问题。

## 变更

- 源码版本：`release/006`，提交 `48df8223`。
- 镜像 tag：`ghcr.io/zeroliao/sub2api:release-006-48df822`。
- 镜像 digest：`ghcr.io/zeroliao/sub2api@sha256:6f5b3e788e500a7d14b37dac2db584205cf81997ed44e575df6101174566c86e`。
- 前端代理快捷添加从正则解析改为标准 `URL` 解析。
- 支持第三方导出的 `http`、`https`、`socks5`、`socks5h` 链接携带 `#备注`。
- 支持 URL 编码的用户名和密码。

## 验收

- `pnpm.cmd --dir .\sub2api\frontend typecheck` 通过。
- 生产镜像构建通过。
- GHCR 候选镜像可拉取。
- `validate-candidate` 通过。
- 蓝绿部署成功。
- 线上 `/health` 返回 `200`。
