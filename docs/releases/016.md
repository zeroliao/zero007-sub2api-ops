# v016 sidecar 代理容器访问地址修复

## 范围

- 状态：已提测
- 类型：源码
- 是否包含 upstream 同步：否
- 触发人：用户要求提交并部署最新本地代码
- 源码分支：`sub2api/dev/016`
- ops 分支：`zero007-sub2api-ops/dev/016`
- 源码提交：`aa75b227affca4734a62c39d542934e292dfbeac`
- ops 提交：`a3fedee84886ca6958483025827da178615c92b6`
- 候选镜像 digest：`ghcr.io/zeroliao/sub2api@sha256:af1e5b536df5665f860d8bae9e35b313800f8abc1c18cef4b6530aa9333f0833`
- 回滚目标：`v015`

## 变更

- 修复订阅源 sidecar 代理默认地址从 `127.0.0.1` 改为 Docker Compose service name `sing-box`，避免 sub2api 容器内连接到自身 localhost。
- 新增 sidecar 地址环境变量：
  - `SUB2API_SIDECAR_LISTEN_HOST`
  - `SUB2API_SIDECAR_PROXY_HOST`
  - `SUB2API_SIDECAR_PROBE_HOST`
  - `SUB2API_SIDECAR_USE_LOCALHOST`
- `proxy_sidecar_endpoints.listen_host` 改为按配置写入，并在冲突更新时同步刷新。
- 新增单元测试覆盖默认 compose 部署、localhost 兼容模式和 probe host 独立覆盖。

## 验证

- `git -C sub2api diff --check -- backend/internal/service/admin_service.go backend/internal/service/admin_service_proxy_subscription_runtime_test.go` 已通过。
- 本机缺少 `go` 命令，`go test ./backend/internal/service -run "TestSidecar|TestIsLocalTCPPortReachable"` 无法执行；后续由 GitHub Actions、镜像构建和服务器验证补齐。
- GHCR Image workflow `26986532330` 已成功，候选镜像 digest 已写入 ops compose。
- `diff-server` 已确认候选差异仅为应用镜像 digest 从 v015 切换到 v016。
- `validate-candidate` 已通过，服务器检出 `zero007-sub2api-ops/release/016 @ a0cebade00694b18505cd93bd2451b182fc25f3b`。
- 备份已完成：`/opt/sub2api-deploy/backups/20260604T235727Z`；蓝绿部署内再次创建 `/opt/sub2api-deploy/backups/20260604T235804Z`。
- 蓝绿部署已完成：run id `20260604T235804Z-bluegreen-deploy-792255`，状态 `succeeded`，exit code `0`。
- 线上健康检查：`GET https://api.zero007.chat/health` 返回 `200` 和 `{"status":"ok"}`。
- 线上 VPN 根路径检查：`GET https://vpn.zero007.chat/` 返回 `404`，未误进入 Sub2API 页面。
- 当前活跃槽位：`blue`。
- 线上容器状态：`sub2api-blue` 使用 v016 digest 且为 `healthy`；`sub2api-caddy`、`sub2api-clash-node`、`sub2api-vpn-ws-node`、`sub2api-postgres`、`sub2api-redis` 均为 `healthy` 或运行正常。
- 线上容器 label 确认 `org.opencontainers.image.revision=aa75b227affca4734a62c39d542934e292dfbeac`。

## 部署记录

- 服务器验证：通过。
- 备份：`/opt/sub2api-deploy/backups/20260604T235727Z`、`/opt/sub2api-deploy/backups/20260604T235804Z`。
- 部署结果：成功，当前活跃槽位 `blue`。
- 说明：生产已切换到候选镜像 `ghcr.io/zeroliao/sub2api@sha256:af1e5b536df5665f860d8bae9e35b313800f8abc1c18cef4b6530aa9333f0833`。
