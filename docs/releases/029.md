# 版本：029

状态：成功
类型：源码 / 混合部署
是否包含 upstream 同步：否
触发人：用户明确要求继续修复并部署

## 涉及仓库

- `sub2api`：是，修复订阅源扫描长请求导致 Cloudflare 524 的问题。
- `zero007-sub2api-ops`：是，更新生产候选应用镜像 digest。

## 版本基线

- 上一成功版本：`v028`
- 源码初始 commit：`0270f770531c70cd1f5cdbd9b3882c7db1d26190`
- ops 初始 commit：`d131621e85e557cfb743539caa220a4e1571489f`
- 开发分支：`dev/029`
- 候选分支：`release/029`

## 变更内容

- 手动订阅源扫描接口改为立即返回 `202 Accepted`，后台使用独立 context 执行扫描。
- 前端通过既有 `scan-status` 接口轮询任务，完成后重新加载扫描结果。
- 保留定时扫描路径，增加异步接口回归测试。

## 验证与候选

- 后端 `go test ./internal/handler/admin -run 'TestProxyHandlerEndpoints' -count=1`：通过。
- 后端 service 编译检查：通过。
- 前端 `pnpm exec vue-tsc --noEmit`：通过。
- 前端扫描 API Vitest：通过。
- 前端 `pnpm run lint:check -- --no-fix`：通过，存在 2 个既有 unused-vars warning。
- 前端 `pnpm run build`：通过；存在既有 chunk/dynamic import warning。
- `git diff --check`：通过。
- GHCR Image workflow：run `32643356296`，成功。
- 源码 release commit：`d818ebd14b2400d0b54c579f6450ee88ac673d4d`。
- 候选镜像 digest：`ghcr.io/zeroliao/sub2api@sha256:c5c82d2235097b9c1bafe15df3e74c38fe6dd324a49adb8d1184d0558afddc74`。
- 本机 Docker exact-digest 验证：待执行；Docker Desktop daemon 当前不可用。
- 服务器候选验证、备份和蓝绿部署：已完成，详见生产结果。

## 生产结果

- ops release commit：`a528b75df6ac6db9bdc6b76369933657feb6e925`。
- `diff-server`：通过；仅应用镜像 digest 与生产当前 compose 不同。
- `validate-candidate`：通过；服务器检出 `release/029 @ a528b75df6ac6db9bdc6b76369933657feb6e925`。
- 备份：`/opt/sub2api-deploy/backups/20260823T140306Z`。
- 蓝绿部署 run：`20260823T140518Z-bluegreen-deploy-3524721`，状态 `succeeded`，exit code `0`。
- active slot：`green`；`sub2api-green` 使用目标 digest 且 healthy，旧 `blue` 槽位已停止。
- 公网验证：`/health`、`/`、`/setup/status` 返回 `200`；未授权 `/api/v1/admin/users` 返回 `401`。
- 生产 Docker exact-digest 拉取、候选启动、Caddy 切流和健康检查：通过。
- SSH 控制通道偶发 banner/app-info 超时，但部署 run、容器状态和公网验证均已完成。
- 回滚目标：`v028`。

## 风险与回滚

- 本版本无数据库迁移。
- 异步扫描状态仍为单进程内存状态；单机蓝绿部署模型下可用，重启会丢失正在运行任务状态。
- 回滚目标：`v028`。
